package org.example.custom;/* Tesco’s content system needs to break large text into readable lines for in‑store displays.The wrapping should be configurable for maximum characters per line.text = "The quick brown fox jumps over the lazy dog"maxWidth = 10["The quick",    // 9 chars "brown fox",    // 9 chars "jumps over",   // 10 chars "the lazy",     // 8 chars "dog"]          // 3 chars*/import java.util.ArrayList;import java.util.Arrays;import java.util.List;public class TextWrapper {    public static void main(String[] args) {        String text = "The quick brown fox jumps over the lazy dog";        int maxWidth = 10;        String text1 = "";        int maxWidth1 = 10;        TextWrapper wrapper = new TextWrapper();        wrapper.wrap(text, maxWidth);        wrapper.wrap(text1, maxWidth1);        wrapper.wrap(                "Supercalifragilisticexpialidocious is a long word", 12);        System.out.println(wrapper.wrap(                "The   quick brown    fox", 10));    }    List<String> wrap(String text, int maxWidth) {//        System.out.println(Arrays.toString(words));        List<String> result = new ArrayList<>();        if(text.isEmpty() || maxWidth <= 0)            return result;        String[] words = text.trim().split("\\s+");        StringBuilder temp = new StringBuilder(words[0]);        for(int i=1;i< words.length;i++) {            String next = words[i];            if(temp.length() + next.length() + 1 > maxWidth) {                result.add(temp.toString());                temp.setLength(0);                temp.append(next);            } else {                (temp).append(" ").append(next);            }        }        if(!temp.isEmpty())            result.add(temp.toString());        System.out.println(Arrays.toString(result.toArray()));        return result;    }}